#include <stdio.h>
#include <string.h>

int main() {
	char str[] = "Hello, World!";
	int length = strlen(str); // Get the length of the string

	printf("Length of str: %d\n", length);

	return 0;
}